function setup() {
  // put setup code here
  createCanvas(480,120);
}

function draw() {
  // put drawing code here
  background(204);
  //rect - quad with four right angles (only needs  four paramaeters: x Pos, y Pos, width, height)
  //x Pos and y Pos always represent upper left corner
  //a square is just a rectangle with equal width and height
  rect(180,60,220,40);

}
